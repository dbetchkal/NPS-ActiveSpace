# Offline dev config — bundled example_data (GLBALSTL 2024-05-24 slice).
# Use: -e GLBA_example
# Run scripts from repo root so relative paths resolve.

[database:overflights]
name =
username =
password =
port =
host =

[data]
nvspl_archive = example_data/nvspl_archive
adsb =
ais = example_data/MXAK-AIS-GLBA
dem =
dem_elevation_units = feet
mennitt =

[project]
dir = example_data/site_projects
nmsim =
FAA_Releasable_db =
FAA_type_corrections =
